#include <stdio.h>
#include <stdlib.h>

int main()
{
    printf("%f\n", 5.0 / 4.0);
    printf("%f", pow(2, 3)); // 2 ** 3
    // sqrt(36)
    // ceil(36.356) // gives next highest number (round up) = 37
    // floor() round down to 36

    return 0;
}
